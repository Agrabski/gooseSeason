To configure:
download and install npm (node package manager)
enter in terminal (gooseSeason directory) :
"npm install"
"npm install -g sass"

To update the css/app.css file, use scss file in folder resources and compile resources/app.scss file using command:
sass resource/app.scss:css/app.css